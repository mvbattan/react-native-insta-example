import { StyleSheet, Dimensions } from 'react-native';

const contentMaxHeight = 100;
const containerBorderColor = 'rgba(0,0,0,0.2)';

const styles = StyleSheet.create({
  
});

export default styles;
